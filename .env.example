# Enables AI photo analysis. This key is used ONLY by the serverless function
# (api/analyze.js) and is never exposed to the browser.
# On Vercel: set this in Project Settings > Environment Variables (no VITE_ prefix).
# For local dev with the function, run `vercel dev` with a .env containing this.
ANTHROPIC_API_KEY=
