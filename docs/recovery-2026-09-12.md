# Source recovery and authentication boundary checkpoint

## Verified sources and hosting

- Recovered Sites source directly from its source repository, not the preview: `70b338e1d4323cc801cba9c7a0b421ec1f4d73b7`.
- Sites project `appgprj_6a9f39a5304481919ef6efc99b520101` remains owner-private. Its saved source can be newer than its published version.
- GitHub connector confirmed repository administrator/read/write access for `MyIntelHome/myintel-assesment`.
- GitHub `main` was `ef0c6ca3dcc13349fe78a2b040663ab6968cc3bd`, a separate Vite application. GitHub `platform` was `442c7777b7e50fcb12052db6d06e05b148c4b1e3` and is an ancestor of the recovered Sites source. Neither remote branch was changed.
- Local recovery branch: `pilot/recover-and-auth-2026-09-12`. The merge preserves the recovered pilot tree and both histories. The older main application and its AI-analysis endpoint remain in upstream history; they are not enabled in the recovered pilot. Do not restore that endpoint into the pilot without reviewing its data flow and claims.
- Vercel browser sign-in succeeded. Existing project: `austin-8620s-projects/myintel-assesment`. Only configured domain shown: `myintel-assesment.vercel.app`, valid configuration, Production. No custom MyIntel domain is configured on this project.
- Current production deployment: `mSZLp9NXCg7QXtQVP21Ye3e5ndWk`, source `platform` at `442c777`, created September 6. The overview says future production pushes use `main`.
- Project preset is Vite; current production overrides are Next.js, `next build`, and `pnpm install --frozen-lockfile`. Project Node version is 24.x; root directory is empty. No deployment checks are configured.
- Project environment variable name visible: `ANTHROPIC_API_KEY` (Production and Preview). Its value was not opened. Storage page has no connected database results. No production auth/database/photo storage credentials have been verified.
- No Vercel settings, domains, deployments, provider accounts, or resident records were changed during these checks.

## Implemented and verified

The shared API now accepts an asynchronous server identity resolver and defaults to anonymous. It no longer parses Sites identity headers. Only the Sites Worker entrypoint selects the explicitly named Sites adapter. A public adapter must verify its provider session and supply a verified principal; session verification failures return a retryable error before any account records are queried. Unverified email addresses cannot become staff through the configured admin email.

Existing account, clinical approval, request ownership, history, and photo checks remain in the shared API. Tests explicitly choose the Sites adapter for preview-specific flows. Nine new boundary tests cover spoofed headers/cookies/bearer strings, failed verification, incomplete principals, staff elevation, clinical restrictions, owner isolation, and origin enforcement.

- TypeScript and all 236 tests pass on Node 24.18.0 (225 after authentication separation, plus 11 database/migration tests).
- Full existing build passes: Next static assets plus bundled Sites Worker and migrations.
- Vercel readiness guard still exits unsuccessfully, as intended.
- No live public authentication, password recovery, production database migration, backup restoration, private-photo integration, or end-to-end production flow is claimed by these tests.

Second tested batch adds `production/database.ts`, `production/migrations.ts`, and `production/snapshot.ts`. The official libSQL client preserves the current SQLite schema and atomic batch behavior. Tests use real local libSQL databases for migration-journal validation, failed-batch rollback, account isolation/idempotency, snapshot verification, and exact empty-target restoration. Production configuration rejects local database fallbacks. See [the migration and rollback runbook](production-data-migration.md) for limits, especially the still-required D1 export/conversion, identity mapping, and private photo-byte transfer. No remote database has been provisioned or migrated.

## Durable save and continuation

- The isolated branch is saved in the existing private Sites source repository as `pilot/recover-and-auth-2026-09-12`. Neither Sites `main` nor the published preview was changed.
- GitHub bulk source-tree creation was rejected by automatic approval review because its payload exceeded the 200,000-byte review limit. The user has been asked to approve individually reviewed file uploads; do not treat elapsed time as approval. One source-image blob was created, but no GitHub branch/tree/commit was published by that failed bulk request. The local and Sites recovery history remains intact.
- A default-branch Sites push was rejected by automatic review for exceeding the isolated-branch scope. The safer isolated-branch push succeeded. Do not retry a default-branch push without the relevant authorization.
- Automation `myintel-bounded-pilot-continuation` was created for one run at 9:45 p.m. America/Denver on September 12. It is bounded to 45 minutes or one tested batch, requires renewed read-only access checks, stops for missing credentials/human gates, and must pause after its attempt. It does not monitor quota or redeem reset credits.
- A live usage read on September 12 showed 91% remaining in the five-hour window and 17% weekly remaining at that instant. Those numbers are historical after the read, not a promise of future allowance. The continuation time follows the user's requested evening reset window.

## Next implementation gates

1. Finish the production database and recovery-sender decisions, then securely connect the existing Supabase project, private bucket and libSQL database to the inspected Vercel project. Keep secrets out of commits and conversation output.
2. Exercise the implemented verified session and production data/storage adapters on an isolated remote stack. Keep Sites header handling exclusive to the Sites entrypoint. Replace the Vercel guard only after actual Vercel backend tests pass.
3. Preserve original Sites user IDs through an explicit, verified account-linking migration; do not merge accounts by an unverified email claim. Back up and rehearse restoration before moving records/photos.
4. Rehearse the implemented named-professional consent, revocation, access auditing and accountable follow-up with authorized test accounts on the remote stack. Do not infer real provider availability from the software flow.
5. Human launch gates remain: actual approved professionals and capacity, clinical-content review, privacy/retention/vendor agreements, senior/family usability sessions, and response ownership. Payments remain disabled; do not send messages without approval.

Read this alongside `pilot-checkpoint.md` and `operating-model.md`. The September 11 statements about unavailable GitHub/Vercel access are historical and superseded by this verification.

## Third tested batch: public account and storage runtime

Added `web-production` reusing the existing UI with Node API routes, Supabase public auth/recovery, encrypted server sessions, strict public-origin checks, and private Supabase photo storage. No Sites identity headers are trusted by this runtime. Payments remain explicitly disabled. Two additional journalled migrations cover sessions, abuse counters and reset revocation.

TypeScript and all 262 tests pass. Both the existing Sites build and the separate server production build pass. Browser inspection of the built public sign-in page confirmed readable controls and an explicit service error when the production configuration is absent, with entered form values preserved. SDK tests use controlled HTTP responses; no live auth email was sent. This is not a complete remote end-to-end or human usability verification.

Supabase and Turso sign-in is pending in the owner's browser tabs. Read `production-account-setup.md` for exact environment names, required token-hash email templates, session protections and remaining launch gates. GitHub transfer remains blocked by the prior automatic review limit; no default branch, Vercel domain or production deployment was changed. Named-professional handoff, live data/photo migration and human launch requirements remain unfinished.

Supabase sign-in subsequently completed. Project `yyueldzkxdcykljbdquz` is healthy in East US (Ohio). Email signup and confirmation were already enabled. Its Site URL was changed from localhost to the verified Vercel origin, the exact `/auth/confirm` redirect was allowlisted, and private bucket `home-photos` was created with a 3 MB limit and `image/jpeg` allowlist. No access policy was added. Custom SMTP is disabled, so sender/delivery readiness and token-hash templates remain launch gates. No API key was revealed, copied or added to Vercel during this step.

## Fourth tested batch: named-professional handoff and follow-up

Staff can link an approved professional account to a separately reviewed provider listing, with revisioned link/unlink audit history. Linking shares no customer data. For a current named proposal, the customer must affirm contact sharing and can separately include the saved home-check snapshot and the exact ready photo IDs present at consent time. The professional endpoint requires current approval, listing link/revision, proposal version, open request and unrevoked consent on every read. Clinical archives never enter the handoff. Customer revocation, staff unlink/relink, professional approval loss, proposal change and request closure end access without relying on the browser.

The professional workspace now has a Shared requests view. Operations shows account links and lets the responsible coordinator save a dated, revision-checked internal follow-up plan. Releasing ownership clears the current plan while retaining audit events. These dates do not send messages or promise response times.

The seventh journalled migration adds the link, consent and follow-up records/events. Application backups now cover all sixteen durable business tables while continuing to exclude active auth sessions and reset/rate-limit state. TypeScript and all 269 tests pass, including real libSQL tests for cross-account isolation, exact photo snapshots, consent revocation, approval/link changes, stale follow-up writes and restore parity. Both builds pass. Live remote-stack testing remains unperformed.

## September 15 production-service checkpoint

GitHub access was reverified for `MyIntelHome/myintel-assesment`; the connected account has administrator/write access. The complete recovered history through `f66cf17` is now published as branch `pilot/recover-and-auth-2026-09-12`. `main` was not moved and no force push was used.

Supabase project `yyueldzkxdcykljbdquz` and Turso database `myintel-production` are provisioned in East US (Ohio). Turso deletion protection is enabled. Seven migrations were applied to the empty database. The database credential was then rotated, saved as a Vercel Secret, and verified by an idempotent migration run with zero pending migrations. The Supabase server key and MyIntel session key were also rotated and saved as Vercel Secrets. `APP_ORIGIN`, Supabase URL/publishable key and private bucket name remain readable production configuration. Vercel's remaining “Needs Attention” item is the credential-free libSQL hostname, which its heuristic classifies as secret-like.

The Sites database overview and bounded row reads found one `case_archives` row, one approved `professional_access` test row and its audit event. The other six legacy tables contain no rows, including `home_photos`, `service_requests`, `providers` and payment records. A temporary owner-private, bearer-secret export route recovered the exact records, then was reverted and redeployed clean with its temporary secret removed. The archive contains six cases at revision 260; the 9,099-byte payload has SHA-256 `8bf3d981d92be4406a94c72bd94f27480f6643c0070bc2bc68ac5039e8ad3cc9`. Supabase currently reports no production users. A verified mapping to `austin@myintelhome.com` and import remain required. The professional approval is test data and is not evidence of real provider availability.

`vercel.json` selects `pnpm run build:production` and `web-production/.next` on the isolated branch. TypeScript, all 269 tests and the server production build passed after that change. Nine branch-scoped variables connect the pilot branch to Turso, Supabase Auth, private storage and the designated administrator email. Deployment `HH7a5KaTiLmyF5MUuzXhRqt6iiHQ` is Ready and the stable branch alias serves the public account flow. The existing public Vercel deployment and domain remain unchanged while legacy import, remote journey tests, reviewed email delivery and human launch decisions remain open.

The exact archive importer adds checksum-pinned source conversion, explicit identity mapping, empty-target enforcement, private before/after snapshots and post-write verification. It deliberately excludes the known test-professional approval from the live database. TypeScript and all 271 tests pass with the importer tests; the live import still waits for a verified Supabase user ID.
